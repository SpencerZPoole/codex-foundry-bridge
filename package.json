{
  "name": "foundry-codex-bridge",
  "version": "0.2.11",
  "private": true,
  "type": "module",
  "description": "Local Codex MCP bridge for a FoundryVTT GM session.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/mcp.js",
    "daemon": "node src/server.js --daemon",
    "smoke": "node test/smoke.js",
    "manifest": "node scripts/generate-capability-manifest.mjs",
    "test": "node test/manifest.js && node test/lifecycle.js && node test/smoke.js",
    "install:module": "powershell -ExecutionPolicy Bypass -File scripts/install-module.ps1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {}
}
